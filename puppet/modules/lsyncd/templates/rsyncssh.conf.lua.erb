# Added by Puppet
sync {
  default.rsyncssh,
  source = "<%= @source %>",
  targetdir = "<%= @targetdir %>",
  host = "<%= @host %>",
  delete = <%= @delete %>,
  delay = <%= @delay %>,

  rsync = {
<% @options.each do |key, val| -%>
    <%= key %> = <%= val %>,
<% end %>
  },
}
